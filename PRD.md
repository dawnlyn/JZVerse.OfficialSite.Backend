# 微服务套件仪表盘功能需求清单

## 一、服务治理可视化

### 1.1 服务全景视图
- 服务卡片网格列表，每张卡片展示：服务名、状态图标、实例数量、健康度百分比
- 服务状态筛选器：运行中/部分异常/严重异常/已下线
- 服务搜索框：支持模糊搜索服务名/标签/负责人
- 服务分组折叠：按业务域/团队/环境分组，支持展开/收起
- 服务详情抽屉：点击卡片从右侧滑出，展示基础元数据
- 实例列表表格：列包含IP、端口、版本、权重、启动时间、状态标签
- 实例状态标签：运行中(绿)/启动中(蓝)/停止中(黄)/异常(红)/未知(灰)
- 服务依赖拓扑图：力导向图，节点为服务，边为调用关系，支持拖拽缩放
- 依赖深度控制器：数字输入框，调节展示层级1-5层
- 依赖方向切换器：单选按钮组，上游依赖/下游被依赖/双向
- 健康度评分仪表盘：环形进度条，0-100分，按分数段变色
- 健康度因子拆解：四个子项进度条（可用率、延迟、错误率、饱和度）
- 服务标签云：标签云展示版本/环境/负责人等标签
- 最近变更时间线：垂直时间轴，展示部署/扩缩容/配置变更记录

### 1.2 流量热力图
- 流量矩阵表格：行=调用方服务，列=被调用方服务
- 单元格颜色映射：按QPS数值映射颜色深浅
- 时间范围选择器：近5分钟/15分钟/1小时/自定义
- 流量类型切换标签：全部/成功/错误/慢调用
- 颜色标尺配置：滑块调节QPS阈值对应的颜色梯度
- 单元格悬浮提示：Tooltip展示QPS数值、P99延迟、错误率
- 迷你趋势图：单元格内嵌Sparkline折线图
- 异常高亮：错误率>5%或P99>1s的单元格红色边框闪烁
- 下钻入口：点击单元格弹出Modal展示调用来源分布饼图

### 1.3 容量水位监控
- 资源仪表盘组：CPU/内存/连接数/线程数/磁盘IO/网络IO
- 仪表盘组件：半圆仪表盘，带当前值、百分比、状态色
- 容量预测曲线：折线图，历史数据+未来7天预测线（虚线）
- 告警阈值线：仪表盘上标记告警线位置
- 瓶颈标记：自动识别并标记接近上限的资源项

---

## 二、配置管理控制台

### 2.1 配置IDE编辑器
- Monaco编辑器集成：支持YAML/JSON/TOML语法高亮
- 代码折叠：按层级折叠/展开配置节点
- 智能代码补全：基于Schema的自动提示下拉框
- 实时语法校验：错误行红色波浪线，悬浮显示错误信息
- 格式化按钮：工具栏一键美化配置格式
- 多标签页：顶部标签栏，支持多文件同时编辑
- 主题切换：亮色/暗色主题切换按钮
- 字体调节：放大/缩小/重置按钮
- 全屏模式：全屏切换按钮
- 行号开关：显示/隐藏行号复选框
- Minimap：右侧代码缩略图导航
- 查找替换：Ctrl+F弹出查找框，支持正则

### 2.2 配置版本管理
- 版本历史列表：表格展示版本号、变更时间、变更人、备注
- 版本对比视图：左右分栏Diff视图
- Diff高亮：新增行绿色背景，删除行红色背景，修改行黄色背景
- 行级差异标记：行内精确标记变更字符
- 回滚按钮：每行版本后的回滚操作按钮
- 标签标记：为版本添加标签输入框
- 备注编辑：行内编辑版本备注
- 版本导出：下载按钮，导出为文件
- 回滚预览：回滚前Modal预览即将应用的变更

### 2.3 配置发布审批
- 审批工作流列表：表格展示待审批/已审批/被驳回状态
- 状态筛选标签：全部/待我审批/我已审批
- 审批详情面板：展示变更内容JSON、申请人、申请理由
- 影响范围列表：展示受影响的服务名称和实例数
- 通过按钮：绿色按钮，确认通过
- 驳回按钮：红色按钮，填写驳回理由文本框
- 转交功能：选择其他审批人下拉框
- 审批历史：时间轴展示该配置的所有审批记录
- 审批规则配置：设置审批层级数和审批人选择器

### 2.4 配置影响分析
- 影响范围图谱：树状图，根节点为配置，子节点为引用服务
- 影响服务卡片：服务名、当前配置版本、实例数量
- 配置引用链：列表展示引用路径（文件->服务->实例）
- 风险评估标签：高/中/低风险等级标签
- 影响实例表格：IP、当前版本、将变更至版本
- 灰度预览：展示灰度发布时优先接收变更的实例列表

---

## 三、流量治理控制台

### 3.1 金丝雀发布
- 任务列表：表格展示任务名、服务、灰度比例、状态
- 新建任务表单：任务名输入、服务选择器、灰度比例滑块(1-100%)
- 灰度标签选择器：Header/Cookie/标签单选
- 规则编辑器：文本域编写匹配规则（如user-id in [1001,1002]）
- 实时监控面板：左右对比灰度实例与稳定实例的流量
- 实例标记：实例列表中灰度实例带"金丝雀"徽章
- 全量发布按钮：一键将灰度比例设为100%
- 回滚按钮：一键将灰度比例设为0%
- 对比报告：错误率/延迟对比柱状图

### 3.2 蓝绿部署
- 环境状态卡片：蓝色环境(旧版本)/绿色环境(新版本)并排展示
- 状态指示器：运行中/预热中/就绪/异常
- 流量切换开关：滑动开关切换流量指向
- 流量比例调节：蓝绿各自百分比输入框（总和100%）
- 环境对比表格：版本号、实例数、健康状态、平均延迟
- 预热进度条：绿色环境预热完成百分比
- 自动切换配置：健康检查通过数阈值输入
- 快速回滚按钮：红色紧急按钮，立即切回蓝色

### 3.3 熔断限流
- 熔断状态网格：服务卡片网格，边框颜色表示状态（绿/黄/红）
- 状态图例：Closed(绿)/Open(红)/Half-Open(黄)
- 熔断规则表单：错误率阈值输入、慢调用阈值输入、熔断时长输入
- 熔断事件时间轴：垂直时间轴标记熔断状态变更
- 限流规则表格：规则名、QPS阈值、并发阈值、状态开关
- 限流效果监控：当前QPS数字、拒绝请求数数字
- 自适应限流开关：Toggle开关
- 热点参数限流：参数名输入框+阈值输入框
- 集群限流配置：集群总QPS限制输入

---

## 四、运维操作中心

### 4.1 服务实例操作
- 实例表格：列包含IP、端口、版本、权重、状态、标签、操作
- 搜索过滤：IP/端口/版本/状态下拉筛选
- 详情抽屉：点击行展开，展示元数据、健康检查历史
- 下线按钮：行内按钮，触发优雅下线
- 上线按钮：行内按钮，重新注册
- 权重滑块：行内滑块调节0-100
- 标签编辑器：行内标签增删改
- 批量操作：表格顶部批量选择后的操作按钮组
- 重启按钮：行内按钮（需二次确认）
- 日志跳转：行内链接跳转日志页面

### 4.2 日志实时检索
- 日志流选择器：应用日志/访问日志/错误日志/自定义下拉
- 时间范围：快捷按钮+自定义时间选择器
- 级别筛选：DEBUG/INFO/WARN/ERROR/FATAL多选框
- 关键字搜索：输入框支持模糊匹配
- 正则模式切换：正则开关
- Live Tail开关：实时滚动开关
- 暂停/继续按钮：控制日志滚动
- 高亮规则：配置关键字和对应颜色
- 上下文展开：点击行展开前后10行
- 导出按钮：下载筛选后的日志文件
- 收藏按钮：星标收藏日志行
- 分享链接：复制当前筛选条件URL

### 4.3 远程诊断
- 线程Dump按钮：触发远程获取
- Dump分析结果：死锁线程红色标记、阻塞线程黄色标记
- 堆内存Dump按钮：带警告提示的触发按钮
- 堆分析表格：类名、实例数、占用内存排序
- GC日志视图：时间轴展示GC事件，标记Full GC
- JVM参数列表：键值对表格展示
- 环境变量列表：键值对表格展示
- 配置项列表：展示当前生效配置
- 远程调试开关：临时开启/关闭调试端口
- Arthas终端：集成Web Terminal

### 4.4 性能剖析
- 任务列表：任务名、服务、实例、状态、操作
- 新建任务表单：服务选择、实例选择、时长输入、频率选择
- CPU火焰图：可交互SVG，点击下钻
- 内存火焰图：分配热点展示
- 锁竞争表格：锁名称、等待线程数、总等待时间
- IO等待图表：IO类型分布饼图
- 方法耗时表格：方法名、平均耗时、调用次数、占比
- 火焰图交互：缩放、还原、搜索方法名
- 报告导出：PDF/HTML格式选择

### 4.5 自动运维
- 扩缩容策略表单：CPU阈值输入、内存阈值输入、QPS阈值输入
- 扩容数量输入：每次增加实例数
- 冷却时间输入：缩容后保护期时长
- 定时任务列表：Cron表达式、动作、状态
- 自愈规则编排：条件选择器+动作选择器
- 自愈动作：重启/摘除/告警/脚本单选
- 自愈历史表格：时间、实例、规则、结果
- 混沌实验表单：故障类型选择（延迟/丢包/宕机）、范围选择、持续时间
- 实验范围：服务选择器+实例选择器+流量比例
- 实验报告：稳定性指标对比图表

---

## 五、开发者门户

### 5.1 API目录
- API列表：路径、方法、名称、所属服务、标签
- 搜索框：路径/名称/标签模糊搜索
- 分组视图：按服务/版本/标签分组
- 详情页面：请求方法Badge、路径、描述
- 参数表格：参数名、类型、必填、默认值、描述
- 请求示例：JSON代码块
- 响应示例：成功/错误Tab切换
- 在线调试：内置HTTP客户端表单
- 参数表单：根据API定义自动生成输入框
- 调试历史：最近10次调试记录
- 收藏按钮：星标收藏
- 订阅开关：变更通知订阅
- 文档导出：Markdown/PDF/OpenAPI按钮

### 5.2 SDK下载
- SDK卡片：语言图标、版本号、安装命令
- 版本选择下拉：历史版本列表
- 复制按钮：一键复制安装命令
- 文档链接：跳转图标
- 更新日志：折叠面板
- 示例代码：语法高亮代码块
- 兼容性表格：SDK版本与服务端版本对应

### 5.3 沙箱环境
- 申请表单：理由文本域、服务多选、时长选择
- 实例列表：沙箱名、状态、到期时间、操作
- 续期按钮：延长时间选择器
- 释放按钮：立即释放确认
- 配置查看：JSON展示沙箱配置
- 日志查看：内嵌日志组件
- 调试入口：Web Terminal链接

### 5.4 接入指南
- 快速开始：步骤编号列表
- 最佳实践：文章列表
- FAQ：可搜索的问答列表
- 视频教程：视频播放器列表
- 代码模板：下载按钮
- 检查清单：复选框列表，逐项确认

---

## 六、可观测性展示

### 6.1 指标监控仪表盘
- 仪表盘列表：名称、创建时间、操作
- 新建仪表盘：空白/模板选择
- 面板添加按钮：打开面板类型选择
- 面板类型：折线图/柱状图/饼图/仪表盘/表格/热力图/数字
- 指标选择器：Prometheus指标下拉搜索
- 标签过滤：job/instance/method等标签键值对输入
- 聚合函数：sum/avg/max/min/rate/increase下拉
- 时间范围覆盖：面板级时间选择器
- 刷新频率：5s/15s/30s/1m下拉
- 阈值线：Y轴阈值输入，虚线显示
- 拖拽排序：面板网格拖拽
- 面板操作：编辑/复制/删除按钮
- 分享：生成只读链接
- 导出：JSON/图片格式

### 6.2 分布式追踪
- 追踪列表：TraceID、根服务、耗时、状态、时间
- 搜索：TraceID输入、服务选择、耗时范围、状态选择
- 瀑布图：水平时间轴，Span层级缩进，不同服务不同颜色
- Span详情：点击Span展开，展示Tags、Logs、耗时分解
- 依赖拓扑：基于追踪数据生成的服务调用图
- 错误筛选：仅显示错误追踪开关
- 慢调用筛选：耗时阈值输入
- 追踪对比：选择两个追踪并排对比
- 分享链接：TraceID分享

### 6.3 日志分析
- 全文检索：搜索输入框
- 结构化展示：JSON字段展开/折叠
- 字段过滤：特定字段值输入过滤
- 频率统计：柱状图展示日志出现趋势
- 模式识别：异常模式列表，带出现次数

---

## 七、系统管理

### 7.1 多租户管理
- 租户列表：名称、成员数、资源使用、配额、操作
- 新建租户：名称输入、配额设置表单
- 配额设置：服务数/实例数/配置数上限输入
- 成员管理：添加成员按钮、成员列表、删除按钮
- 角色分配：管理员/开发者/观察者下拉
- 资源统计：折线图展示使用量趋势
- 成本分摊：表格展示各租户成本
- 隔离策略：网络隔离/数据隔离级别选择

### 7.2 权限管理
- 用户列表：用户名、邮箱、角色、状态、操作
- 角色列表：角色名、权限数、描述、操作
- 权限矩阵：行=权限，列=角色，单元格=复选框
- 资源授权：树形选择器选择服务/配置/数据
- 操作授权：查看/编辑/删除复选框组
- API权限：API路径列表，操作列复选框
- 数据权限：部门/团队范围选择器

### 7.3 系统设置
- 参数配置：键值对表格，支持编辑
- 通知渠道：邮件/短信/钉钉/企业微信配置表单
- 告警联系人：姓名、联系方式、值班状态
- 规则模板：预置规则列表，应用按钮
- 日志保留：天数输入、归档开关
- 备份配置：备份时间、保留份数输入
- 版本信息：各组件版本号列表
- 健康检查：一键检查按钮、结果列表